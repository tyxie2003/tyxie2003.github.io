---
layout: post
title: "教你谈正常恋爱"
author: "Tianyi Xie" 
---

学校教你很多东西但是没教的更多。学校不会教你如何谈恋爱，如何理财，如何过好一辈子。这并非学校的错，而更多的是我们不学你懂吗。

<span style="color:#5da164"> CATEGORIES </span>

        <hr>
        <ul class="sidebarlist">
          {% assign all_categories = "" %}
          {% for post in site.posts %}
            {% for category in post.categories %}
              {% assign all_categories = all_categories | append: category | append: "," %}
            {% endfor %}
          {% endfor %}
          {% assign categorieses = all_categories | split: "," | uniq | sort %}
          {% for category in categorieses %}
            {% if category != "" %}
              <li><a href="{{ site.baseurl }}/category/{{ category | downcase | replace: ' ', '-' }}">{{ category }}</a></li>
            {% endif %}
          {% endfor %}
        </ul>


​        <!-- <span style="color:#5da164"> TAGS </span>

        <hr>
        <ul class="sidebarlist">
          {% assign all_tags = "" %}
          {% for post in site.posts %}
            {% for tag in post.tags %}
              {% assign all_tags = all_tags | append: tag | append: "," %}
            {% endfor %}
          {% endfor %}
          {% assign tags = all_tags | split: "," | uniq | sort %}
          {% for tag in tags %}
            {% if tag != "" %}
              <li><a href="{{ site.baseurl }}/tag/{{ tag | downcase | replace: ' ', '-' }}">{{ tag }}</a></li>
            {% endif %}
          {% endfor %}
        </ul> -->


​        <span style="color:#5da164"> AUTHORS </span>

        <hr>
        <ul class="sidebarlist">
          {% assign authors = site.posts | map: 'author' | uniq | sort %}
          {% for author in authors %}
            <li><a href="{{ site.baseurl }}/author/{{ author | downcase | replace: ' ', '-' }}">{{ author }}</a></li>
          {% endfor %}
        </ul>